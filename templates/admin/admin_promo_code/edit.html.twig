{% extends 'base.html.twig' %}

{% block title %}Edit PromoCode
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<div class="row">
			<div class="col-md-8 offset-md-2">
				<div class="card shadow-sm">
					<div class="card-header bg-warning text-dark">
						<h1 class="h3 mb-0">✏️ Edit Promo Code</h1>
					</div>
					<div class="card-body">
						{{ include('admin/admin_promo_code/_form.html.twig', {'button_label': 'Update'}) }}
					</div>
					<div class="card-footer d-flex justify-content-between align-items-center">
						<a href="{{ path('app_promo_code_index') }}" class="btn btn-secondary">
							⬅️ Back to List
						</a>
						{{ include('admin/admin_promo_code/_delete_form.html.twig') }}
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
